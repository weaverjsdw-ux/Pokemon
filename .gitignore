# Local config with addresses + webhooks. NEVER commit.
config.yaml
config.local.yaml
.env
.env.*
!.env.example

# SDD workspace — per-plan ledgers, task briefs, implementer reports, review packages.
# Short-lived scratch: git history is the durable record. (.claude/skills/poke-sdd/ writes here.)
.superpowers/

# Runtime state
*.db
*.db-journal
*.sqlite
*.sqlite3
data/stores.cache.json
data/*.cache.json
logs/

# Python
__pycache__/
.pytest_cache/
.pytest_tmp/
.pytest_tmp*/
.tmp_costco_*
.tmp_pytest_*/
pytest-cache-files-*/
.tmp_web_*.log
*.py[cod]
*$py.class
.venv/
venv/
.python-version
*.egg-info/

# Backups & local scratch (added 2026-06-14 — service-plan Phase 0)
*.bak
*.orig
sprint*.txt
iddoctor_offline.txt
test_baseline.txt

# Editor
.vscode/
.idea/
.DS_Store

# /poke rendered dashboards are build output (regenerate via `python -m scanner.discovery.render`)
dashboards/

# Money Hypothesis Lab runtime ledgers — append-only, disposable, regenerated at
# runtime (never committed; local evidence files).
data/poke/price_history.jsonl
data/poke/paper_decisions.jsonl
data/poke/verified_candidates.jsonl
# Phase G gem-rate evidence ledger (append-only, provenance-stamped; a committed result
# doc is the durable proof, mirroring the gitignored price-history ledger).
data/poke/gem_rates.jsonl
