# Local config with addresses + webhooks. NEVER commit.
config.yaml
config.local.yaml
.env
.env.*
!.env.example

# Runtime state
*.db
*.db-journal
*.sqlite
*.sqlite3
data/stores.cache.json
data/*.cache.json
logs/

# Python
__pycache__/
.pytest_cache/
.pytest_tmp/
.pytest_tmp*/
.tmp_costco_*
.tmp_pytest_*/
pytest-cache-files-*/
.tmp_web_*.log
*.py[cod]
*$py.class
.venv/
venv/
.python-version
*.egg-info/

# Backups & local scratch (added 2026-06-14 — service-plan Phase 0)
*.bak
*.orig
sprint*.txt
iddoctor_offline.txt
test_baseline.txt

# Editor
.vscode/
.idea/
.DS_Store

# /poke rendered dashboards are build output (regenerate via `python -m scanner.discovery.render`)
dashboards/

# Money Hypothesis Lab runtime ledgers — append-only, disposable, regenerated at
# runtime (never committed; like price_history.jsonl they are local evidence files).
data/poke/paper_decisions.jsonl
data/poke/verified_candidates.jsonl
